# 快速启动指南

## 项目结构

```
finance-system/
├── frontend/          # React 前端项目
│   ├── src/
│   │   ├── pages/     # 页面组件
│   │   ├── components/# 通用组件
│   │   ├── App.tsx    # 应用入口
│   │   └── main.tsx   # React 挂载点
│   ├── package.json
│   └── vite.config.ts
├── backend/           # Node.js 后端项目
│   ├── src/
│   │   └── index.ts   # 后端入口
│   ├── prisma/
│   │   └── schema.prisma # 数据库模型
│   ├── package.json
│   └── tsconfig.json
└── docs/              # 文档
```

## 🚀 本地运行

### 1. 安装依赖

**后端：**
```bash
cd backend
npm install
```

**前端：**
```bash
cd frontend
npm install
```

### 2. 初始化数据库

```bash
cd backend
npx prisma generate
npx prisma migrate dev
```

### 3. 启动服务

**启动后端（端口 3001）：**
```bash
cd backend
npm run dev
```

**启动前端（端口 3000）：**
```bash
cd frontend
npm run dev
```

### 4. 访问应用

- 前端：http://localhost:3000
- 后端 API：http://localhost:3001/api
- Prisma Studio（数据库管理）：`npx prisma studio`

## 📱 功能模块

### ✅ 已实现

**前端界面：**
- [x] 仪表盘 - 财务概览、各公司统计
- [x] 财务流水 - 录入、查看、筛选
- [x] 开票管理 - 发票列表、新增发票
- [x] 报表中心 - 利润表、现金流、公司营收、支出构成

**后端：**
- [x] Express 服务器搭建
- [x] Prisma 数据库模型设计
- [ ] API 路由实现（待开发）
- [ ] 用户认证（待开发）

### 🚧 待开发

- [ ] 用户登录/注册
- [ ] API 完整实现
- [ ] PDF 发票自动生成
- [ ] 多币种汇率同步
- [ ] Excel 导出
- [ ] 数据持久化

## 🎨 技术特点

### 性能优化
- React Query 数据缓存
- 代码分割（Code Splitting）
- 懒加载
- 响应式设计

### 用户体验
- 简洁干净的界面
- 快速响应（目标 < 500ms）
- 多公司支持
- 多币种支持

## 📦 部署

### Vercel 部署（推荐）

**前端：**
1. 连接 GitHub 仓库
2. 在 Vercel 创建新项目
3. 选择 `frontend` 目录
4. 自动部署

**后端（Vercel Serverless Functions）：**
1. 将后端代码迁移到 Vercel Functions
2. 配置环境变量
3. 部署

### 服务器部署

**传统服务器：**
1. 上传代码到服务器
2. 安装依赖
3. 使用 PM2 管理进程
4. 配置 Nginx 反向代理

## 🔧 开发命令

**前端：**
```bash
npm run dev      # 开发服务器
npm run build    # 生产构建
npm run preview  # 预览生产构建
```

**后端：**
```bash
npm run dev              # 开发服务器
npm run build            # 构建 TypeScript
npm start                # 启动生产服务器
npx prisma studio        # 打开数据库管理界面
npx prisma migrate dev   # 创建数据库迁移
npx prisma generate      # 生成 Prisma Client
```

## 📝 下一步

1. **完善后端 API** - 实现所有 CRUD 操作
2. **用户认证** - 添加登录/注册功能
3. **PDF 生成** - 实现自动发票生成
4. **部署上线** - 部署到 Vercel
5. **测试** - 全面测试功能

---

*Developed by 小龙虾 ღ( ´･ᴗ･` )*
